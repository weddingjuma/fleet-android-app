<resources>

    <!-- Base application theme. -->
    <style name="AppTheme" parent="MapotempoTheme">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimaryDark">@color/colorMapoBlue</item>
        <item name="android:textColorPrimary">@color/colorDarkGrey</item>
    </style>

    <style name="AppTheme.Night" parent="MapotempoTheme.Night">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="colorPrimaryDark">@color/colorMapoBlue</item>
        <item name="android:textColorPrimary">@color/colorDarkGrey</item>
    </style>

    <!--<style name="AppTheme.AppBarOverlay" parent="ThemeOverlay.AppCompat.Dark.ActionBar" />-->

    <!--<style name="AppTheme.PopupOverlay" parent="ThemeOverlay.AppCompat.Light" />-->

</resources>
